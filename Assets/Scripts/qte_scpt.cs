using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class qte_scpt : MonoBehaviour
{
    // Start is called before the first frame update
    private int degree = 0;
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {

        degree += 10;

        
    }
}
